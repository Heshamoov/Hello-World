<%= form_with(model: guid, local: true) do |form| %>
  <% if guid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guid.errors.count, "error") %> prohibited this guid from being saved:</h2>

      <ul>
      <% guid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
